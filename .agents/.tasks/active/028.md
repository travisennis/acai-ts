---
id: 028
title: Remove dead commented-out code from bash.ts
status: Open
priority: P3
effort: S
labels: type:chore, area:tools
exec_plan: -
depends_on: -
created: 2026-06-15T19:20:54-04:00
---
# Remove dead commented-out code from bash.ts

## Problem

`source/tools/bash.ts` contains ~250 lines of commented-out code:
- Lines 22-236: a massive commented-out JavaScript template literal (the former tool description) replaced by the concise `simpleDescription` variable.
- Lines 690-760: the entire `getInstalledTools()` function (~60 lines) is commented out.

This adds noise, increases file size, and creates confusion about which description is canonical.

## Relevant files

- `source/tools/bash.ts` — commented-out blocks

## Implementation notes

1. Remove the large commented-out description block (lines 22-236, currently the value at the top of the file).
2. Remove the commented-out `getInstalledTools` function.
3. Ensure `simpleDescription` is still the active tool description.
4. Verify no other code references the removed content.
5. Do NOT change any other code in the file.

## Acceptance notes

- No commented-out code blocks remain in bash.ts.
- The Bash tool still works with the same schema and behavior.
- All tests pass.
- `npm run check` passes.

e the step data that the TUI needs rather than the full array.
4. Simplest fix: `this._state.steps = []` after the while loop exits, before yielding `agent-stop`.

## Acceptance notes

- `this._state.steps` does not grow unboundedly within a single agent run.
- Existing tests pass.
- TUI display of tool execution is unaffected (it renders from live events, not step history).
- `npm run check` passes.

