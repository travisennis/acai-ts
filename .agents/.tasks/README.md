# Task Artifacts

This directory stores task artifacts for acai-ts. For the full workflow, read `.agents/TASKS.md`.

Use `.agents/.tasks/index.md` as the manually maintained queue and create individual task files beside it with stable three-digit ids, such as `001.md`.
